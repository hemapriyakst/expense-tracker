package com.studentexpense.tracker.entity.enums;

/**
 * EXPENSE -> money leaving a wallet
 * TOPUP   -> money entering a wallet (e.g. received cash, bank transfer)
 */
public enum TransactionType {
    EXPENSE,
    TOPUP
}
